from django.apps import AppConfig

class KanyarasiConfig(AppConfig):
    name = 'kanyarasi'
    verbose_name = 'Kanyarasi Application'

    # You can add app-specific configuration here. For most projects,
    # this file remains quite simple and is often left untouched,
    # but you can override methods like ready() if you need to do
    # something when the app is loaded.
